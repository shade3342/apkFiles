# StockSignal APK

시그널 트레이딩(StockSignal) 디버그 빌드 APK 배포용 리포지토리.

## 설치 방법 (실제 폰)

1. 아래 파일을 폰에서 다운로드
   - [`stocksignal-debug.apk`](stocksignal-debug.apk)
2. 폰 설정에서 **출처를 알 수 없는 앱 설치 허용** (Chrome/파일 앱 등)
3. 다운로드한 APK 를 눌러 설치

> ⚠️ **디버그 빌드**입니다. 디버그 서명 키로 서명돼 있어 테스트용입니다.
> 스토어 배포용이 아닙니다.

## 파일

| 파일 | 빌드 | 비고 |
|---|---|---|
| `stocksignal-debug.apk` | debug | **Stage 2 「수익 래칫(PROFIT_RATCHET)」 청산 사유의 앱 표기를 신규 배선했다 — 서버가 이미 이 코드를 내려보내고 있어 앱이 몰랐다면 «오표기 4번째 사고»가 났을 자리다.** 종전엔 서버가 새 청산 사유 코드를 추가하면 앱은 그 코드를 몰라 **가격 추론 폴백**으로 떨어졌는데, 그 폴백은 되밀림폭이 작을 때(전체의 95% 이상) **「전략의 매도 조건이 충족되어 청산됐습니다」라는 완전히 틀린 문구**를 낸다(같은 유형 사고가 119850 종목에서 2026-07-30·08-02·08-26 세 번 반복됐다). 이번 빌드는 `PROFIT_RATCHET` 을 매도 사유 사전에 미리 등록해, 손익 부호에 따라 「수익 확정 · 번 것을 지킴」(이익) 또는 「고점 되밀림 · 조기 정리」(손실)로 정확히 갈라 표기한다. ⚠️ **이 사유는 서버에서 아직 다크(관측만·라이브 미발동) 상태**라 지금은 실제로 뜨지 않지만, 서버 활성화가 앱보다 먼저 나가면 그 순간부터 오표기가 시작되므로 **활성화 전에 선행 배포**했다. 🚫 알림 문구·발송 개수·채널 구성 무접촉. 🛡️ 스키마 변경 없음(순수 표기 사전 추가). / **(직전) **청산 사유 표기를 «손익 부호»로 정확히 가르도록 보완했다(직전 빌드의 결함 수정).** 직전 APK 는 「이익 보존(PROFIT_PRESERVE)」 사유를 **항상 이익으로** 표기했는데, 이 사유는 이름과 달리 **손실 청산에도 붙는다** — 고점에서 벌었던 이익 기준으로 발동하지만 급락 갭이나 체결 지연으로 실제 매도가가 진입가 아래로 미끄러질 수 있기 때문이다. 그대로 두면 **마이너스로 끝난 거래가 「이익을 지켰다」로 설명**되는 셈이라, 이제 수익률 부호를 보고 갈라 적는다 — 이익이면 「이익 보존 · 작은 수익 확정」, 손실이면 「고점 되밀림 · 손실 확대 차단」, 수익률을 알 수 없으면 중립 표기(「고점 되밀림 · 보존선 도달」)로 떨어진다. 강조색도 같은 기준으로 갈라, **손실 카드가 이익처럼 강조되지 않는다.** 표기 어휘는 성과 검증 화면과 **같은 소스**를 쓰므로 두 화면이 같은 청산을 다르게 설명할 수 없다. 🚫 푸시 알림 문구·홈 카드는 여전히 무접촉이며 알림 개수·시간·채널 구성도 변화 없다. 🛡️ 기존 알림 이력 보존(DB v14 유지 — 이번 빌드는 스키마 변경 없음). / **(직전) 「시그널 상세」 화면에서 이익보존 청산이 손절처럼 보이던 결함 수정 — 손실로 끝난 이익보존 청산에 「이익을 지켰다」는 강조색이 붙지 않게 했다.** 직전 빌드(성과 검증 화면)에서는 청산 사유(PROFIT_PRESERVE 등)를 **수익률 부호와 함께** 판정해 익절/손절을 구분했는데, **「시그널 상세」 화면(홈 카드→시그널 클릭)만 부호를 무시하는 구버전 로직을 그대로 쓰고 있었다.** PROFIT_PRESERVE(이익보존선)는 이름과 달리 **손실 청산에도 붙을 수 있다** — 무장은 고점 대비 이익 기준으로 되는데 실제 체결가는 갭·체결 지연으로 그 아래까지 미끄러질 수 있어서다. 부호를 무시하면 이런 마이너스 청산에도 「이익 보존」이라는 강조색이 붙어 **손실이 이익처럼 보이는 착시**가 생긴다. 이제 두 화면(성과 검증·시그널 상세)이 **완전히 같은 판정 로직**을 쓴다: 수익률이 양수면 「이익 보존 · 작은 수익 확정」(강조), 음수면 「고점 되밀림 · 손실 확대 차단」(중립)으로 갈린다. 🛡️ **수익률 값을 못 읽으면 중립 제목·약한 색으로 폴백**한다(있지도 않은 「이익」을 지어내지 않는다). 부수로 서버가 보내는 청산 사유 코드 **10종 전부**(PROFIT_PRESERVE·DECAY_TP 포함)를 계약 테스트로 고정해, 서버가 새 사유 코드를 추가했는데 앱이 그 표기를 깜빡 빠뜨리는 사고를 미리 막았다. 알림 문구·발송 개수·채널 구성은 변화 없다. / **(직전) **매도 알림 상세에 「왜 팔았는지」가 표시된다 — 그리고 «이긴 거래»가 손절처럼 읽히지 않게 했다.** 종전에는 알림을 눌러 들어간 상세에서 **무엇이 청산을 발동시켰는지 알 수 없었다**(성과 검증 화면에는 있었지만 알림 상세에는 없었다). 이제 서버가 청산 사유 코드를 함께 보내고, 상세에 한 줄로 나온다 — 예: `청산 사유 · 이익 확정 · 고점 되밀림 감지`. ★**표현을 신중히 골랐다**: 가장 흔한 청산 유형은 「목표를 넘겨 상승을 따라가다 고점에서 −3% 되밀릴 때 파는 것」인데 실측상 **평균 +7.66%로 이긴 거래**다. 이걸 「−3% 되밀려 잘림」처럼 기계적으로 쓰면 **익절이 손절로 읽힌다** — 그래서 이익을 지킨 계열은 「이익 확정」·「이익 보존」으로 적고 강조색을 주며, 손절·추세 이탈과 시각적으로 구분한다. 🚫 **푸시 알림 문구와 홈 카드 목록은 한 글자도 바뀌지 않았다**(알림 한 줄이 길어지면 정작 「매도 추천」이 잘려 나간다). 표기 어휘는 성과 검증 화면과 **같은 소스**를 쓰므로 같은 청산이 두 화면에서 다르게 설명될 수 없다. 모르는 사유 코드가 오면 **그 줄을 아예 그리지 않는다**(추측해서 지어내지 않는다). 🛡️ **기존 알림 이력은 그대로 보존된다**(DB v13→v14 무손실 — 컬럼만 추가). ⚠️ 다만 **이번 업데이트 이후 도착하는 매도 알림부터** 사유가 표시된다(과거 알림에는 그 정보가 없다). 알림 개수·시간·채널 구성은 변화 없다. / **(직전) 손절 알림의 「계획된」 표기를 «검증 가능할 때만» 붙이도록 고치고, 상세 화면에 「매수 시 안내」를 추가했다.** ①**거짓 표기 교정(중요)** — 종전 손절 알림은 「**계획된** 손절선 −2.0% 도달」이라고 단정했는데, 그 숫자는 매수 시점이 아니라 **팔리는 시점의 시장 국면**에서 다시 계산된 값이었다. 즉 **한 번도 계획된 적 없는 숫자를 「계획된」이라 부르고 있었다.** 실측(코스피 1,030거래일): 보유 1일이면 **19.2%**, 3일이면 **35.9%**가 매수 때 안내한 값과 다르고, 방향은 「예고보다 **더** 잃음」이 20.4%로 더 흔하다. 이제 앱이 매수 때 안내한 값을 **포지션에 저장해 두었다가 대조**해서, 두 값이 같을 때만 「계획된 손절선 …」이라 쓰고 다르거나 기록이 없으면 「손절선 …」으로 쓴다. **숫자 자체는 어느 경우에도 실제로 잘린 선**이라 항상 사실이다. ②**상세 화면에 「매수 시 안내」 한 줄 추가** — 알림을 눌러 들어가면 `매수 시 안내 · 손절 −3.0% · 목표 +6.0%`가 보이고, 실제 적용된 값이 다르면 `→ 청산 시점(폭락장) 기준 손절 −2.0% 적용`이 함께 뜬다. 「왜 숫자가 다른가」를 **우리가 먼저 밝히는 것**이며, 손절선이 시장 국면에 맞춰 조정된다는 설명도 함께 나온다. 🚫 **푸시 알림과 홈 카드 목록에는 넣지 않았다** — 알림 한 줄이 길어지면 정작 「매도 추천」이 화면에서 잘려 나가서다(접힌 알림은 약 28자까지만 보인다). ③함께 넣은 안전장치 — 목표가를 되짚을 때는 **「목표는 추세가 꺾이면 먼저 익절해 도달 전에 청산될 수 있습니다」**를 반드시 같이 보여 준다. 실측상 목표가에 실제로 도달해 익절되는 경우는 **200건 중 1건(0.5%)** 뿐이고 대개 그 전에 고점 되밀림으로 **평균 +7.66%**에서 먼저 익절되기 때문에, 목표만 보이면 **이긴 거래가 「목표 미달」로 잘못 읽힌다.** 🛡️ **기존 알림 이력은 그대로 보존된다**(DB v12→v13 무손실 — 컬럼만 추가). 알림 개수·시간·채널 구성은 **하나도 바뀌지 않았다.** / **(직전) [핫픽스 2차] 앱 화면의 장세 국면 표기 교정.** 직전 핫픽스는 «알림» 쪽에만 「(발송 시점)」을 붙여서, 정작 앱 홈 배너는 아무 시점 표기가 없어 **여전히 「어느 쪽이 지금인가」가 모호**했다. ①배너 캡션을 「시장 국면」 → **「현재 시장 국면」**으로 바꿔 알림(과거 기록)과 배너(실시간)의 역할을 분명히 했다. ②**별개 결함 하나를 더 찾아 함께 고쳤다** — 배너는 실시간 국면을 못 받아오면 **최대 24시간 전 알림에 실린 국면으로 조용히 되돌아가면서 그것을 「현재」인 것처럼** 표시하고 있었다. 이제 그 경우엔 캡션이 **「최근 관측 국면」**으로 바뀌어, 낡은 값을 지금이라고 말하지 않는다(값을 숨기지는 않는다 — 아무것도 안 보이는 것보다 «언제 것인지 밝힌 값»이 낫다). / **(직전) [핫픽스] 알림 표시 결함 4건 수정 — 실기기 확인 후 즉시 조치.** ①**「목표」를 먼저, 「손절」을 나중에** — 종전엔 「손절 -6.0% · 목표 +20.0%」 순서라 매수 추천인데 **가장 먼저 읽는 단어가 「손절」**이 되어 손실 경고처럼 각인됐다. 알림 본문과 헤더 **양쪽 다** 순서를 뒤집었다. ②**매도 알림에 매수용 문구가 붙던 오작동** — 한국콜마 «익절 청산» 알림 하단에 「기준을 높여 종목을 엄선하고 있습니다」(= 신규로 살 종목을 고르는 행위)가 붙어 나갔다. 이미 판 종목에 붙을 말이 아니다. 매도용 문장(**「시장을 주시하며 다음 기회를 준비합니다」**)으로 갈랐다. ③**국면 표시가 앱 화면과 어긋나 보이던 문제** — 푸시는 「하락장」인데 같은 무렵 홈 배너는 「횡보장」이었다. **두 화면이 다른 데이터를 보는 게 아니었다**(같은 문서를 본다). 실측해 보니 그날 오전 국면이 **2↔4 로 다섯 번 뒤집혔다** — 코스피가 20일선(약 6,466)에 딱 붙어 진동했고 판정식에 완충대가 없어 분 단위 틱에 넘어갔다. 익절 알림이 나간 09:16 은 「하락장」 구간 «안»이었고, 배너를 보신 시각은 「횡보장」으로 되돌아온 뒤였다 — **둘 다 그 순간엔 맞았다.** 그래서 값을 억지로 맞추는 대신 알림에 **「(발송 시점)」** 을 명시해 «알림 = 그때의 기록 / 배너 = 지금»임을 분명히 했다. ⚠️ 국면이 몇 분 사이 뒤집히는 것 자체는 **매매에도 영향을 주는 별도 결함**이라(신규매수 차단·손절선이 함께 흔들린다) 근본 수정은 승인 안건으로 올려 두었다. ④**푸시 알림 줄바꿈이 뭉개지던 문제** — 알림이 여러 건 쌓여 자동으로 접히면 표시 줄 수가 제한되는데 **빈 줄이 그 자리를 정보 없이 차지**해 뒤쪽 내용(국면·면책)이 잘려 나갔다. 빈 줄을 없애고 이모지를 구분자로 삼았으며, **접힌 상태에서도 「국면 · 목표 · 손절」이 한 줄로 보이도록** 헤더를 바꿨다(종전엔 국면과 목표·손절 중 하나만 보였다). / **(직전) 하락장 알림 «턴어라운드» — 손절 알림을 «사고»가 아니라 «약속대로 작동한 방어»로 읽히게 했다.** 7~8월 하락장에서 손절이 폭주하며(손절률 68%·당일청산 72%) 이탈 압력이 커졌는데, 감사 결론은 **손실 «횟수»가 아니라 「예고 없이 맞았다」는 설명의 부재**가 원인이라는 것이었다. 🚫 알림을 줄이는 해법은 쓰지 않았다 — **알림 수량은 1건도 바뀌지 않는다**(발송 여부·대상·빈도 로직 무접촉). ①**알림 채널 3분리** — 종전엔 채널이 하나뿐이라 알림이 피곤할 때 남은 선택지가 «앱 알림 전체 끄기»뿐이었다. 이제 **매수 추천 / 청산 알림 / 요약·소식** 으로 나뉘어 폰 설정에서 부분 제어가 된다. ★**익절과 손절을 «한 채널»에 묶은 것이 핵심 설계**다 — 따로 두면 「매수 추천은 계속 받고 손절 통보만 끄기」가 가능해지는데, 이 앱은 우리가 대신 팔아주지 않으므로 **청산 알림은 매수 추천의 나머지 반쪽**이다. 게다가 사용자가 그 채널을 끌 시점은 정확히 폭락장 손절 연타일이라, 최악의 순간에 최악의 선택을 원클릭으로 주는 꼴이 된다. 안전망으로 **청산 알림이 꺼져 있으면 시그널 홈 최상단에 상시 경고**가 뜨고 그 배너를 누르면 바로 복구된다. ②**매수 알림에 손절선·목표가 사전 고지** — 「손절 12,300원(-3.0%) · 목표 14,200원(+12.0%)」. 살 때 약속한 적이 없으면 팔 때 「약속대로」라고 말할 수 없기 때문에 ③보다 이쪽이 먼저다. ★함께 나가는 2행 **「현재 국면 기준 · 목표 전 추세가 꺾이면 먼저 익절」**이 중요하다 — 실측상 하드 익절이 실제로 발동한 건 **청산 200건 중 1건(0.5%)**뿐이고 거의 항상 러너 트레일링이 먼저 걸리는데(평균 **+7.66%**), 목표가만 보이면 **우리가 이긴 거래가 「목표의 1/4에서 쫓겨난 것」으로 읽힌다.** ③**손절 알림 문구 교체** — 「하드 손절 안전장치, 매도 추천」 → **「손절선 -3.0% 도달, 매도 추천」**. ⚠️ 초안은 「**사전 안내한** 손절선」이었으나 **기각**했다: 그 숫자는 매수 시점이 아니라 «청산 시점 국면»에서 다시 계산되므로, 하락장(-3%)에서 산 종목이 폭락장 전환 후 손절되면 **한 번도 본 적 없는 -2.0%**를 통보받게 된다. 게다가 그 오차는 **국면 전환일 = 손절 폭주일**에 100% 집중된다(가장 많은 사람이 가장 화나 있는 날에 반증 가능한 거짓말을 하는 셈). 매수 시점 고지값을 저장하도록 서버를 고친 뒤에야 「안내드린 손절선」으로 승격한다. ④**국면 배지** — 하락장·폭락장일 때만 알림과 카드에 국면을 표시해 「내 종목이 나빴다」가 아니라 「시장 전체가 어렵다」는 맥락을 준다(강세장·횡보장엔 표시하지 않는다 — 매번 붙는 상수는 사용자가 «읽지 않는 법»을 학습해 정작 폭락장에 띄웠을 때도 안 보게 된다). ⑤**폭락장 배너 문구 교체** — 초안 「손절선을 좁혀 리스크를 관리하고 있습니다」는 **기각**했다. 사용자에겐 「더 자주·더 빨리 손실을 확정한다」로 번역되고 그 번역이 정확해서, 손절 알림 연타와 겹치면 배너가 변명이 아니라 **자백**이 된다(내부 용어를 그대로 화면에 올린 실수). → **「기준을 높여 종목을 엄선하고 있습니다」** + **「폭락장에도 매수 추천은 계속됩니다 — 오늘 최상위 1종」** 2행. 뒤 문장이 「폭락장인데 알림이 안 오네 = 앱이 죽었네」를 선제 차단한다. ⑥**「관리하는 앱」 포지셔닝** — 스플래시·프리셋 마켓 카피를 「시그널을 준다」에서 「진입부터 청산까지 관리한다」로 조정. 🛡️ **알림 이력은 보존된다** — DB v11→v12 는 컬럼 추가만 하는 무손실 마이그레이션이라 기존 알림이 지워지지 않는다(과거 업데이트마다 알림이 전멸하던 사고의 재발 방지 정책을 그대로 지켰다). 검증: 빌드 성공 · 단위테스트 40/40 · 마이그레이션 테스트 에뮬 통과 · 서버 9함수 배포 완료. ⚠️ 알림 렌더는 제조사(One UI 등)마다 달라 에뮬레이터로 판정이 불가능하니, **채널 3분리와 배지는 실기기에서 확인이 필요**하다. / **(직전) 알림 아이콘 재조정 — 획을 가늘게 · 점을 작게 «떼어내» 앱 아이콘과 같은 형상으로 · 바탕은 옆 앱들 크기로.** 직전 빌드의 아이콘이 「브이와 점이 붙어 있고 점이 너무 크다」는 지적을 받았고, 확대해 보니 셋 다 사실이었다. ①**바탕이 작았다** — 실측 우리 32px vs 알리익스프레스 38.7px·옆 아이콘 37px → 배지를 아이콘 상자의 83%에서 **96%**로 키웠다(모서리 곡률도 함께 조정). ②**획이 두꺼웠다** — 배지 대비 16%였는데 알리익스프레스 스마일 획은 약 10.5%다 → **9.7%로 39% 얇게**. ③**점이 크고 획에 «융합»돼 있었다(못생김의 정체)** — 직전 판은 「브이자 끝에 점이 오도록」이라는 지시를 «획 끝점에 원을 겹쳐 한 몸으로 합치기»로 구현해 획 끝이 부푼 **올챙이 모양**이 됐다. 앱 아이콘은 점이 «획의 연장선 위에 **떨어져**» 있다 → 앱 아이콘의 실제 비율(점 지름 ÷ 획 두께 = 1.25, 간격 ÷ 획 두께 = 0.575)을 역산해 그대로 이식했다(**점 반지름 −48%**, 간격은 작은 크기에서 안티에일리어싱에 먹히지 않도록 0.71로 살짝 과장). 형상 자체가 런처 앱 아이콘 좌표를 축소해 배지 중앙에 놓은 것이라 **이제 앱 아이콘과 완전히 같은 형상**이다. 밝은 상태바 = 검정 배지 + 배경색 브이 / 어두운 상태바 = 흰 배지 + 검정 브이 자동 반전은 그대로. / **(직전) 세로 스크롤 인디케이터 전 화면 도입 — 「지금 목록의 어디쯤인가」가 보이게 했다.** 시그널홈·성과검증처럼 카드가 수십 장 쌓이는 화면에서 **현재 위치를 가늠할 단서가 아예 없던** 문제. 조사해 보니 이건 «요즘 디자인 트렌드»가 아니라 **Compose 가 빠뜨린 것**이었다 — 안드로이드 구 View 시스템(ScrollView·RecyclerView)은 「스크롤 중 표시 → 멈추면 사라짐」을 **기본 제공**했고 iOS 도 동일한데, Compose 는 그걸 안 들고 왔다(현 stable material3 1.4.0 에 스크롤바 API **0건**, foundation 1.10.4 도 배관만 있고 그리는 기능 없음 — AAR 직접 열어 확인). 구글도 되돌리는 중이다(foundation 1.10~1.11 에 걸쳐 API 신설). ⇒ **「요즘 앱은 스크롤바를 안 쓴다」는 트렌드는 존재하지 않으며**, 실제 트렌드는 «상시 노출 막대»가 아니라 **«스크롤 중에만 뜨는 얇은 반투명 막대»** 라서 그 형태로 만들었다. 적용 **10곳**(시그널홈·내 전략[성과검증·운용중]·프리셋 마켓·조건식 생성·지표 선택 시트·마이·시장 탐색·전략 상세·온보딩·약관 시트). 손을 떼면 0.65초 뒤 스르르 사라지고, **스크롤할 내용이 없으면 아예 그리지 않는다.** 콘텐츠 «위에» 덧그리는 방식이라 **레이아웃·터치 동작에는 영향이 0**이며, 애니메이션이 화면 리컴포지션을 유발하지 않아 스크롤 성능도 그대로다. ⚠️ 공식 API(`Modifier.scrollIndicator`)를 쓰려면 Compose 버전을 한 단계 올려야 하는데 그러면 제스처·텍스트·스크롤 동작 전반이 회귀 위험에 노출돼, 출시 전 시점에서는 **의존성을 건드리지 않는 자체 구현**을 택했다(주인님 선택). 에뮬레이터 실측 검증: 스크롤 중 표시 · 정지 시 사라짐 · 맨 아래에서 막대가 끝까지 도달 · 고정된 채 콘텐츠와 함께 밀려나지 않음. / **(직전) 알림 상태바 아이콘을 「검정 배지 + V 녹아웃」으로 재설계 + 성과검증 종목 검색 추가.** ①**알림 아이콘(같은 지시 3회차 — 앞선 두 번은 실제로 반영되지 않았다)**: 폰 상단에 뜨던 아이콘이 종전엔 «배지 없는 실루엣»이었고 비콘 점도 브이 획 끝에서 약 1.5px 떠 있었다. 이번에 **앱 아이콘과 같은 둥근 네모 배지를 불투명으로 채우고 그 안의 V·비콘을 «투명하게 뚫는(녹아웃)» 구조**로 재작성했고, 비콘은 V 오른쪽 획의 **끝점에 중심을 두고 획과 하나로 이어지게** 붙였다. 상태바 small icon 은 알파(모양)만 렌더되고 색은 OS 가 칠하므로 → **밝은 상태바 = 검정 배지 + 배경색 V / 어두운 상태바 = 흰 배지 + 검정 V** 로 **리소스 하나에서 자동 반전**된다(유튜브·미래에셋 M STOCK 아이콘이 정확히 이 구조다). ⚠️ **2026-07-30 판단 정정**: 그때 「배경을 넣으면 전 픽셀이 불투명해져 흰 사각형으로 뭉개진다」며 배경을 기각했는데, 그 말은 «색을 칠한 배경»에만 맞고 «불투명 배지 + 투명 녹아웃»에는 해당하지 않는다(모양이 구멍으로 남으므로 뭉개지지 않는다) — 그 오판 때문에 같은 지시가 두 번 미구현으로 끝났다. ②**성과 검증 종목 검색**: 기본 조회기간이 1개월이라 카드가 너무 많이 쌓이는 문제에, 상단에 **종목명·종목코드 검색바**를 신설했다(부분 일치·입력 즉시 필터, 공백 제거+소문자 비교라 「삼성 전자」·「5930」 같은 입력도 걸린다). 위치는 **세그먼트 토글 바로 아래·리스트 «밖»** — 기간 칩은 스크롤하면 사라지지만 검색은 「카드가 많다」를 푸는 수단이라 목록을 한참 내린 상태에서도 손이 닿아야 하기 때문이며, '성과 검증' 탭이고 이력이 있을 때만 나타나 다른 화면에는 영향이 없다. 머티리얼 기본 입력창(최소 56dp) 대신 40dp 로 압축해 카드 가시 영역을 지켰고, 결과가 0건이면 **원인을 검색어/기간으로 나눠 안내 + 「검색 해제」 버튼**을 준다. / **(직전) 성과 검증 청산 안내 문구 3종 신설 — 「왜 팔았는지」를 정확히 말하게 했다.** 종전엔 성격이 전혀 다른 방어장치들이 전부 "트레일링 스탑" 또는 "추세 이탈" **한 문구로 뭉개져** 나갔다. 실제 사고: 지엔씨에너지(119850) 2026-07-30 청산은 **러너 하드컷**(목표 수익선을 넘겨 상승을 따라가다 고점 대비 −3% 되밀림)이었는데 화면엔 "ATR 트레일링"으로 표기됐고, 심지어 **"최고가 32,600원 대비 0.0% 되밀린 시점에 청산"** 이라는 수학적 모순 문구가 나갔다(고점을 찍고 판 건데 되밀림이 0%). 이번 빌드에서 ①청산 사유 3종(🏃 **이익 확정·고점 되밀림 감지** / 🛟 **이익 보존·되밀림 상한** / 🕞 **장 마감 전 정리·되돌림**)을 문구로 분리해 각 방어장치가 자기 이름으로 설명되게 했고, ②서버가 모르는 코드를 보내도 기존 가격 기반 설명으로 안전하게 내려앉도록 폴백을 유지했다(하위호환). ⚠️ **동반 서버 수술(배포 완료·앱 바이너리와 무관):** 「0.0% 되밀림」의 진짜 원인은 앱이 아니라 **서버가 앱에 내려주던 최고가 자체가 틀린 것**이었다 — 성과검증 최고가 동기화가 10분 스로틀에 굶어 진입가에 머문 채 청산되면, 논리모순 방지용 클램프가 `max(진입가, 매도가)`를 최고가로 확정해 **최고가 = 매도가**가 됐다(전수 실측 청산 225건 중 89건 = 39.6%). 이제 청산 순간 **엔진이 판정에 실제로 쓴 고점(HWM)** 을 함께 기록한다(추가 조회·쓰기·KIS 호출 **0** — 이미 메모리에 있는 값). 🔎 **주의: 이 수정은 «앞으로 발생하는 청산»부터 적용된다.** 이미 저장된 과거 카드(7/30 지엔씨에너지 포함)는 그 시점 값으로 굳어 있어 **앱을 재설치해도 바뀌지 않는다**(서버 문서를 다시 받아올 뿐이다). / **(직전) 누락 Pro 조건식 3종 복원 + 기준값 [숫자]/[지표] 토글 신설(교차 비교 개방) — ①조건식 지표 시트가 기획 25종(무료 5 + Pro 20) 중 22종만 노출하고 있었다. 다중 라인 지표를 «대표 스칼라 1개»로 요약하면서 잘려나간 3종(**MACD 시그널선 · 스토캐스틱 %D · 볼린저 밴드폭**)을 정식 지표로 승격, 짝이 되는 원 지표 바로 옆에 배치(소실 원인 자체가 짝 라인의 시야 분리였다). ②권한 매핑 버그 원복 — **이격도·엔벨로프 %B** 가 Pro 뱃지 없이 무료로 풀려 있던 것을 잠금(무료는 현재가·거래량·SMA·EMA·볼린저밴드 %B 딱 5종). ③**기준 값 입력을 [숫자]/[지표] 2모드**로 확장 — 종전엔 숫자 키보드 전용이라 「현재가 ≥ 이동평균선(20일)」·「MACD 가 시그널선을 돌파」 같은 **지표 대 지표 비교를 만들 수단이 아예 없었다**(엔진은 이미 지원, 입력 수단만 부재). 방어 4종 동반: **Pro 우회 차단**(참조로 Pro 지표를 뒷문 사용하는 매출 구멍 봉쇄 — 잠긴 지표 선택 시 페이월) · 자기 참조 제외 · 미완성 참조 저장 차단 · 미리보기와 '내 전략' 카드가 **같은 포맷터**를 써 «@MA:20» 같은 내부 문법이 화면에 새지 않도록 통일(그 결과 미리보기 숫자도 카드와 동일한 천 단위 표기로 정렬). ④동반 백엔드 수술(배포 완료·앱 바이너리와 무관): 직전 봉 지표(`PreviousSnapshot.metrics`)가 없어 확장 보조지표 19종의 **'상향/하향 돌파'가 영원히 발동하지 않던** 결함 해소 — 오류 로그조차 없는 조용한 no-op 이었다. 에뮬 콜드부팅 실측 검증(3종 🔒Pro 노출·토글 전환·페이월 차단·「현재가 ≥ 이동평균선(SMA)(20일)」 미리보기·기간 드롭다운 자동 노출).** / **(직전) 마이 › 서비스 이용약관 시트 흔들림 버그 수정 — 약관 팝업을 맨 아래까지 스크롤한 뒤 계속 쓸어올리면 팝업 전체가 위아래로 떨리던 증상 해소. 원인은 Compose ModalBottomSheet 의 알려진 nested-scroll 결함으로, 본문 스크롤이 경계에 닿으면 남은 스크롤 델타·플링 속도가 부모인 시트로 전달돼 시트가 앵커를 벗어나 끌렸다 되튕긴다(에뮬 A/B 프레임 캡처로 확정 — 수정 전엔 경계 플링 중 시트가 약 41dp 솟았다 복귀, 수정 후 12프레임 픽셀 동일). 조치 ①verticalScroll 바깥에 nestedScroll 차단막을 두어 onPostScroll/onPostFling 잔여분 흡수 ②판정은 scrollState.value > 0 — 맨 위에서는 그대로 흘려보내 '위에서 아래로 끌어 닫기' 제스처 보존(회귀 방지) ③경계 stretch 오버스크롤 제거. 회귀 검증: 맨 아래 드래그=안 닫히고 본문만 스크롤 / 맨 위 드래그=정상 닫힘.** / **(직전) 알림 상태바 아이콘을 앱아이콘(V+점)으로 통일 — 폰 알림 시 상단에 뜨는 작은 아이콘을 종전 번개(⚡)에서 현재 앱아이콘과 같은 「체크(브이) 획 + 그 위 점」 모양으로 교체(ic_stat_signal.xml). 상태바 small icon 은 알파(모양)만 렌더되는 단색 실루엣이라 OS 가 상태바 테마에 맞춰 자동 틴트 → 어두운 상태바=흰 V·흰 점 / 밝은·다크반전 상태바=검은 V·검은 점 이 별도 리소스 없이 충족됨. 참조 리소스명 동일이라 코드 변경 0.** / **(직전) 구독 결제 시트 홍보 박스 폴리싱(오픈특가 분리 뱃지·전문 아이콘·정중 문체) — 결제 확인 바텀시트 혜택 홍보 박스에서 ①오픈 기념 특가 문구를 본문과 분리해 accent 솔리드 필 뱃지(pill·포인트 컬러 배경+대비 텍스트+불꽃 아이콘)로 승격해 시선 강조, ②헤드라인 이모지를 전문 퀀트앱 톤 Material 아이콘(Insights/Verified/WorkspacePremium)으로 교체, ③문체를 ~입니다체로 통일. (직전 빌드의 홍보 박스 신설·구독 통합테스트 검증 포함)** / **(직전) 구독 결제 혜택 홍보 박스 추가 + 개별단품/티어 구독 통합테스트 완료 — ①결제 확인 바텀시트에 경고 문구 위로 요금제별 혜택 홍보 박스(헤드라인 + 즉시성 카피 + 오픈특가/무료체험 라인)를 노출해 구매 심리 촉진. 문체는 전문 ~입니다체, 헤드라인은 이모지 대신 전문 퀀트앱 톤 Material 아이콘(Pro=Insights·Preset Pack=Verified·All-in-One=WorkspacePremium, FOMO=불꽃·체험=선물 아이콘). ②개별 프리미엄 단품 3종 + 티어 요금제 3종(Pro/Preset Pack/All-in-One) 구독 → 가상 시그널 수신을 에뮬레이터 실구동으로 관통 통합테스트 완주(6종 전부 구독 상태 생성·granular 게이팅[산 것만 수신·안 산 건 차단]·구독 시 프리미엄 전략 내 전략 자동 등록 확인). 실 결제 로직은 불변(테스트용 우회 하네스는 검증 후 전부 제거·실 결제 코드 복원).** / **(직전) PresetBadge 공통화·소프트 모노크롬 + 콤팩트화 + 종목코드 타이포 통일 — ①'내 전략' 뱃지를 앱 전역 공통 컴포넌트(PresetBadge)로 추출해 시그널 홈의 파란 레거시 뱃지를 전면 교체(UI 파편화 제거). ②고대비 흑백을 눈이 편한 소프트 모노크롬으로 완화(PRO: Dark=실버#D1D5DB+다크슬레이트#1F2937 / Light=소프트차콜#404859+오프화이트#EDEFF2, 무료=슬레이트 Dot). ③뱃지 사이즈의 힘 빼기(패딩10/4→6/2·이름11→10sp·PRO 9→8sp·dot5→4dp) — 종목명이 주연·뱃지는 콤팩트 꼬리표. ④시그널 홈 종목 표기를 성과 검증과 통일: "삼성전자(005930)" 단일텍스트 → [종목명 13sp·강조]+여백+[코드 11sp·톤다운 회색](괄호 제거). ⑤시그널 홈 카드 상하여백 압축(패딩10→6·간격5→3·카드간격7→5). 에뮬 다크·라이트 실측 확인.** / **(직전) 성과 검증 전략 꼬리표 무료/유료 시각 차별화 + 카드 수직 압축(정보 밀도↑) — '성과 검증' 목록·상세의 전략 꼬리표 뱃지를 무채색·고대비 프로 트레이딩 톤으로 재설계(이모지 🏷️·블루 틴트 제거): 무료(Standard)=슬레이트 무채색 틴트+미니멀 회색 Dot, 유료(Pro/Premium)=묵직한 솔리드 배지(Light=딥차콜+흰글자·Dark=실버+진한글자, AppPalette.isDark 테마 동적)+우측 끝 얇은 외곽선 'PRO' 마이크로칩. presetId→isPremium 로 티어 동적 판별. 더불어 성과 카드 상하 패딩(py 9→6, 좌우보다 타이트)·섹션 세로간격 50% 축소(제목↔뱃지 4→2·뱃지↔가격 6→3·진입↔매도 2→1)·보조데이터(종목코드·진입/매도 날짜·가격 라벨) 폰트 12→11sp+행간 축소로 한 화면 종목 수↑.** / **(직전) 성과 검증 현재가 실시간 반영 — 성과 검증 탭을 열어둔 채로 있어도 서버가 갱신한 현재가가 자동으로 따라오도록 `signal_history_meta/global` 단일 doc 스냅샷 리스너 추가(탭이 보일 때만 attach·벗어나면 detach·유휴 read 0). 탭에 머물면 화면이 이전 값에 '멈춘 것처럼 보이던' 체감 문제 해소.** / **(직전) 시그널 홈 '오늘' 필터를 달력상 오늘(기기 로컬 자정) 기준으로 수정 — 롤링 24시간 방식이던 이전 로직 탓에 어제 늦은 시각 알림이 오늘 목록에 섞이던 문제 해소.** / **(직전) 카드 UI 하드코어 다이어트 — 시그널 홈 반복 면책문 카드에서 제거(리스트 하단 1회로 이동)·프리셋 뱃지+현재가 한 행 배치, 내 전략 성과검증 카드 세로여백 극단 압축(패딩 12→9·간격 10→6·PriceCell 2→1)** / **(직전) 리스트 카드 콤팩트 압축(정보 밀도 강화)** — 트레이딩 앱답게 스크롤을 줄이도록 시그널 홈·성과 검증·운용중 카드의 과한 여백을 다이어트. 카드 간격(10/14→7/8dp)·내부 패딩(16→14/10~12dp)·본문 줄높이(21→16sp)를 줄이고, 홈 카드는 발동 현재가를 액션 문구와 같은 행으로 이동(세로 1줄 절약), 성과 카드는 종목명↔3단 가격 사이 빈 여백을 축소. 에뮬 실측 결과 한 화면에 홈 4장·성과 3장 노출(색/뱃지 정책 불변: 익절=레드·손실=블루·익절/규칙청산 뱃지·최고가 🔥). (동반: 백엔드 동일프리셋·동일종목 중복진입[피라미딩] 차단 **HEAD 정합 복구**는 functions 소스 정합분·앱 UI 무관.) / **(동반) 구독 해지 정책 안내 + 마이페이지 상태 자동 최신화**(이 빌드에 함께 포함) — 마이 탭·프리셋 마켓 [구독 관리 및 해지] 아래 해지 정책 안내 캡션 + 마이페이지 진입 시 Play 온디바이스 로컬 조회로 요금제 상태 즉시 최신화(서버 호출 0). / **(직전) 매도 뱃지 파란색 완전 제거(익절=풀레드)** — 익절(수익률>0) 매도 카드는 좌상단 '매도' 배지 자체(배경+글씨)까지 레드로 통일해 +수익 카드에서 파란 기운을 완전히 지움(손실/미상 매도만 블루). 홈 시그널 카드·시그널 상세 양쪽 적용. / **(직전) 매도 뱃지 파란색 완전 제거(익절=풀레드)** — 익절(수익률>0) 매도 카드는 좌상단 '매도' 배지 자체(배경+글씨)까지 레드로 통일해 +수익 카드에서 파란 기운을 완전히 지움(손실/미상 매도만 블루). 홈 시그널 카드·시그널 상세 양쪽 적용. (동반: 백엔드 동일프리셋·동일종목 중복진입[피라미딩] 차단은 functions 배포분 — 앱 UI 무관.) / **(직전) 성과검증 청산사유 하이브리드 + 전략 꼬리표 + 손익기반 뱃지/컬러 폴리싱** — ①성과 상세 팝업 청산사유를 백엔드 실제 사유코드(익절/손절/트레일링/추세이탈) 우선 표기·구버전은 가격추론 폴백(하이브리드). ②목록카드·상세 양쪽에 전략(프리셋) 꼬리표(🏷️) + 미청산 최고가 갱신 뱃지(🔥). ③청산 뱃지를 손익 기준으로 세분화 — 수익=**"익절"·레드**, 손실/본전=**"규칙 청산"·회색**('손절' 단어 미사용). ④'파란색 공포증' 교정: 익절(+수익) 매도 카드의 본문 하이라이트·매도가를 한국 관례대로 **레드**로(매도 액션 배지 배경은 유지). (백엔드 최고가<매도가 논리모순 픽스는 functions 배포분) / **(직전) 날씨 위젯 등락률 줄바꿈(%) 수정** — 코스피처럼 값이 긴 종목(7,000선)에서 "+5.15%"의 `%`가 다음 줄로 내려가던 문제 해결. 값+등락률을 단일 BasicText(2색)로 합치고 maxLines=1·softWrap=false·autoSize(9~12sp)로 폰 폭에 맞춰 자동 축소 → 어떤 폰 포맷에서도 한 줄 보장(1080·720 폭 실측). / **(직전) 시장 국면 판정 완화 + 날씨/장세 시간축 분리** — 시그널 홈 상단 코스피 '맑음(오늘 상승)'인데 하단 배너 '폭락장'으로 뜨던 인지 부조화 조치. ①상단 위젯에 "오늘의 증시 · 당일 등락", 하단 배너에 "시장 국면" 캡션을 넣어 **당일 등락 vs 며칠~몇 주 추세**를 시간축으로 구분. ②백엔드 국면 판정 완화(재배포됨): 20일선 아래라도 5일선 회복 시 폭락 즉시 해제→반등장(B1), 폭락은 200일선 이탈 또는 (변동성≥45% AND 20일선 대비 -10% 낙폭) 동시충족일 때만(B3, 변동성 단독 폭락 금지). 오토트레이더 인버스 트리거(30%)는 불변. (직전 빌드의 성과검증 청산 안내 매도 케이스별 문구 정확화도 포함) |
